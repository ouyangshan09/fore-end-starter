import React from 'react';
import './Account.scss';
interface IProps {
}
declare class Account extends React.PureComponent<IProps, any> {
    static displayName: string;
    constructor(props: IProps);
    render(): JSX.Element;
}
export default Account;
